import React from "react";
import { render } from "@testing-library/react";
import AddClinicalView from "./add-clinical-view.component";

describe("<AddClinicalView/>", () => {
  beforeEach(() => {
    render(<AddClinicalView />);
  });

  it("should render without dying", () => {
    pending();
  });
});
